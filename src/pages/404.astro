---
import Layout from "../layouts/Layout.astro"
import Navbar from "../components/Navbar.astro"
import Footer from "../components/Footer.astro"
import { url } from "../utils/url"
---

<Layout title="404 - Página no encontrada | Jose Salas">
  <Navbar />
  <main class="min-h-screen flex items-center pt-16">
    <div class="max-w-5xl mx-auto px-6 w-full">

      <div class="grid md:grid-cols-2 gap-16 items-center">

        <!-- Número 404 grande -->
        <div
          class="animate-fade-up select-none overflow-hidden"
          style="animation-delay: 0.1s; opacity: 0; animation-fill-mode: forwards;"
        >
        <p class="font-bold leading-none text-gray-100 dark:text-gray-800 tracking-tighter" style="font-size: 8rem;">
            404
        </p>
        </div>

        <!-- Texto y CTA -->
        <div>
          <div
            class="animate-fade-up inline-flex items-center gap-2 mb-6 px-3 py-1 rounded-full border border-gray-100 dark:border-gray-800"
            style="animation-delay: 0.2s; opacity: 0; animation-fill-mode: forwards;"
          >
            <span class="w-2 h-2 rounded-full bg-red-400"></span>
            <span class="text-sm text-gray-500 dark:text-gray-400">Página no encontrada</span>
          </div>

          <h1
            class="animate-fade-up text-3xl font-bold text-gray-900 dark:text-gray-100 tracking-tight mb-4"
            style="animation-delay: 0.3s; opacity: 0; animation-fill-mode: forwards;"
          >
            Esto no debería estar aquí.
          </h1>

          <p
            class="animate-fade-up text-gray-500 dark:text-gray-400 leading-relaxed mb-8"
            style="animation-delay: 0.4s; opacity: 0; animation-fill-mode: forwards;"
          >
            La página que buscas no existe o ha sido movida. Puede que hayas
            escrito mal la URL o que el enlace esté roto.
          </p>

          <div
            class="animate-fade-up flex flex-wrap gap-3"
            style="animation-delay: 0.5s; opacity: 0; animation-fill-mode: forwards;"
          >
            <a
              href={url("")}
              class="px-5 py-2.5 bg-gray-900 text-white text-sm font-medium rounded-full hover:bg-gray-700 transition-colors dark:bg-white dark:text-gray-900 dark:hover:bg-gray-200"
            >
              Volver al inicio
            </a>
            <a
              href={url("blog")}
              class="px-5 py-2.5 border border-gray-200 text-gray-700 text-sm font-medium rounded-full hover:border-gray-400 transition-colors dark:border-gray-700 dark:text-gray-300 dark:hover:border-gray-500"
            >
              Ver el blog
            </a>
          </div>
        </div>

      </div>
    </div>
  </main>
  <Footer />
</Layout>