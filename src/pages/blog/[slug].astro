---
import Layout from "../../layouts/Layout.astro"
import Navbar from "../../components/Navbar.astro"
import Footer from "../../components/Footer.astro"
import { getCollection } from "astro:content"
import { url } from "../../utils/url"

export async function getStaticPaths() {
  const posts = await getCollection("blog")
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }))
}

const { post } = Astro.props
const { Content } = await post.render()
---

<Layout title={`${post.data.title} — Jose Salas`} description={post.data.description}>
  <Navbar />
  <main class="max-w-2xl mx-auto px-6 py-32">

    <!-- Cabecera del post -->
    <header class="mb-12">
      <div class="flex flex-wrap gap-2 mb-4">
        {post.data.tags.map((tag) => (
          <span class="px-2 py-0.5 text-xs text-gray-500 bg-gray-50 border border-gray-100 rounded-full dark:bg-gray-800 dark:border-gray-700 dark:text-gray-400">
            {tag}
          </span>
        ))}
      </div>

      <h1 class="text-4xl font-bold text-gray-900 leading-tight mb-4 dark:text-gray-100">
        {post.data.title}
      </h1>

      <time class="text-sm text-gray-400">
        {post.data.date.toLocaleDateString("es-ES", {
          year: "numeric",
          month: "long",
          day: "numeric",
        })}
      </time>
    </header>

    <!-- Contenido del post -->
    <div class="prose prose-gray max-w-none dark:prose-invert">
      <Content />
    </div>

    <!-- Volver al blog -->
    <a href={url("blog")} class="inline-block mt-12 text-sm text-gray-400 hover:text-gray-700 transition-colors dark:hover:text-gray-200">
      ← Volver al blog
    </a>

  </main>
  <Footer />
</Layout>